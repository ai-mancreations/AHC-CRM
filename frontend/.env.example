# The frontend calls the backend via the /api path, proxied by Vite in dev
# (see vite.config.ts) and by your hosting platform's rewrite rules in
# production (see render.yaml). No frontend env vars are required by default.
